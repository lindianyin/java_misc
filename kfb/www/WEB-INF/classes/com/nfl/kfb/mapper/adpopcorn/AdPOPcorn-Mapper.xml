<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nfl.kfb.mapper.adpopcorn.AdPOPcornMapper">

	<insert id="insertAdPOPcorn" parameterType="map">
		INSERT INTO
			adpopcorn (
				REWARD_KEY
				, USN
				, ITEM_KEY
				, QUANTITY
				, MEDIA_KEY
				, CAMPAIGN_KEY
				, EPOCH
		)
		VALUES (
			#{rewardKey}
			, #{usn}
			, #{itemKey}
			, #{quantity}
			, #{mediaKey}
			, #{campaignKey}
			, #{epoch}
		)
	</insert>
	
</mapper>